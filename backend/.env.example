# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=kalenda
DB_PASSWORD=kalenda
DB_NAME=kalenda

# Solana Configuration
SOLANA_NETWORK=devnet
SOLANA_RPC_ENDPOINT=https://api.devnet.solana.com

# JWT Secret (for any future authentication needs)
JWT_SECRET=kalenda_secret_key

# Email Service (for notifications)
EMAIL_SERVICE=smtp
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=kalenda@example.com
EMAIL_PASS=your_email_password